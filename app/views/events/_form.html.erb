<%= form_for(@event) do |f| %>
  <% if @event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@event.errors.count, "error") %> prohibited this event from being saved:</h2>

      <ul>
      <% @event.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
	<div class="input">
	  <%= f.text_field :name %>	  
	</div>
  </div>
  <div class="field">
    <%= f.label :search_string %>
	<div class="input">
	  <%= f.text_field :search_string %>	  
	</div>
  </div>
  <div class="field">
    <%= f.label :from_date %>
	<div class="input">
	  <%= f.datetime_select :from_date %>	  
	</div>
  </div>
  <div class="field">
    <%= f.label :to_date %>
	<div class="input">
	  <%= f.datetime_select :to_date %>	  
	</div>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
